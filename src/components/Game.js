import styled from 'styled-components';

const Game = () => {
	return <StyledGame></StyledGame>;
};

export default Game;

const StyledGame = styled.div`
	background-color: black;
	min-height: 100vh;
	color: white;
`;
